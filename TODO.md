0.2版：
1、 全局变量在函数外部初始化(即定义时初始化)，会失败!!! 原因尚不清楚，在函数中初始化或定义就不会出现问题。
【OK】错误原因：ELF文件中data数据段页对齐所致，参考 tools/build.c

2、注意：console.c优化会出错!!! 即使是 -O
此问题在0.2版时尚未完全解决。但将clean_line()中的内嵌汇编的输出部分，添加 "=D"(start) 可以解决其中一个问题。

3、还是优化问题，当优化选项为 -O1 或 -O2 或 -O3 等时，会导致系统无法正常启动。
此问题在0.2版时尚未完全解决。只知道原因是开启上述优化选项之后，在ld时，会将main()连接在head模块之前，导致无法正常启动。但解决办法还没有。